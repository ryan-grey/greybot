# Install with --no-deps: this package requires discord.py[voice], whose extra caps
# PyNaCl below the release pinned in requirements.txt. Its real dependencies are pinned there.
discord-ext-voice-recv==0.5.2a179
